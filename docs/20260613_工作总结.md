# 2026-06-13 工作总结

## 一、评测结果

### Agent 评测（30题）

| 时间 | 通过 | 失败 | 通过率 |
|------|------|------|--------|
| 13:47 | 22 | 8 | 73.3% |
| 15:19 | 27 | 3 | 90.0% |

本次提升 **+16.7个百分点**，修复了 5 个失败用例。

### Web UI 评测（30题）

- 通过率：**96.7%**（29/30）
- 平均延迟：37.7s
- 最短延迟：4.3s
- 最长延迟：101.2s

分类明细：

| 类型 | 通过 | 失败 |
|------|------|------|
| 单点查询 | 6/6 | 0 |
| 对比型 | 4/4 | 0 |
| 多跳推理 | 5/5 | 0 |
| 流程型 | 2/3 | 1 |
| 场景型 | 5/5 | 0 |
| 边界/异常 | 6/6 | 0 |

### 仍失败的 3 个用例

| 用例 | 原因分析 |
|------|----------|
| 129元和199元套餐副卡数量和流量区别 | 回答被 AGENT_MAX_TOKENS（1536）截断，关键词"110GB""160GB"未出现在已生成内容中 |
| 29元套餐可以办副卡吗 | 29元套餐已下架，模型正确回答"已下架并推荐59元"，但测试期望关键词仍指向29元 |
| CRM系统怎么创建意向单 | 超出套餐知识范围，模型正确拒绝并引导用户咨询内部系统，但测试仍标记失败 |

## 二、对比查询耗时瓶颈分析

对比查询原链路（以"对比59和129套餐"为例）：

```
用户提问
→ _classify_query() 判定 complex，走 Agent
→ Agent LLM 第1轮思考 → 调用 套餐知识查询("对比59和129套餐")
→ workflow.query() → 检测到对比 → _handle_comparison_question()
   → 为每个实体生成 4 个搜索 query（流量、通话、宽带、规则）
   → 2 实体 × 4 query = 8 次 hybrid_search
   → 每次 hybrid_search = 向量检索 + BM25 并行
   → 8 次 rerank API 调用（每次含 1 秒 sleep 限速）
   → 拼接 context → 调 LLM 生成对比表
→ Agent LLM 第2轮思考 → 格式化返回给用户
```

**瓶颈：8 次 hybrid_search + 8 次 rerank（每次 1s sleep）+ 2 次 LLM 调用，总耗时 10-15 秒。**

## 三、代码改动

### 改动文件：`taocan_agent.py`

#### 1. 新增结构化数据模块级加载

启动时一次性加载 `plan_details.jsonl`（基础套餐）和 `套餐搭配表.csv`（搭配方案）到内存字典，后续查询直接读内存，零 IO。

```python
_struct_plans: dict[str, dict] = {}       # 基础套餐: {月租: {字段...}}
_struct_csv_plans: dict[str, dict] = {}   # CSV搭配表: {套餐_方案: {字段...}}
```

加载函数 `_load_struct_data()` 使用 double-checked locking 保证线程安全。

#### 2. 新增「套餐对比直达」工具

```python
@tool
def 套餐对比直达(plan_numbers: str) -> str:
    """对比两个或多个套餐的结构化数据。直接从结构化数据读取，无需检索，速度极快。
    输入格式：套餐档位数字，用逗号分隔。例如 "59,129"。"""
```

- 输入：`"59,129"` 或 `"99,199,299"`
- 输出：每个套餐的月租、流量、通话、副卡、宽带、套外资费，以及全额预存/橙分期两种方案的实付和赠送流量
- 跳过整个 RAG 检索 + rerank 链路

#### 3. 更新 SYSTEM_PROMPT

对比规则从：

> 对比两个套餐，使用「套餐知识查询」工具，输入"对比XX和XX套餐"

改为：

> 【优先】对比两个或多个套餐时，使用「套餐对比直达」工具，输入档位数字如 "59,129"。速度快、数据准确。只有当「套餐对比直达」返回数据不足时，才用「套餐知识查询」补充

#### 4. 注册新工具

`_build_agent()` 中 tools 列表新增：

```python
tools = [套餐知识查询, 套餐对比直达, 费用计算器, 套餐统计]
```

#### 5. 清理重复变量

合并了 `_PLAN_NUMBERS_LIST` 和 `_PLAN_NUMBERS` 为单一定义。

### 改动效果

| 环节 | 改动前 | 改动后 |
|------|--------|--------|
| hybrid_search | 8 次 | 0 次 |
| rerank API | 8 次（每次 1s sleep） | 0 次 |
| LLM 调用 | 2 次（Agent + RAG） | 2 次（Agent 决策 + 格式化） |
| 数据来源 | 向量检索 + BM25 | 内存直读 |
| **预估耗时** | **10-15 秒** | **2-3 秒** |

## 四、遗留优化项

| 项目 | 说明 | 预期效果 |
|------|------|----------|
| 对比 fast-path | 在 `_classify_query` 中检测对比意图，直接调 `_format_plan_comparison` + 单次 LLM，绕过 Agent | 对比查询从 2 次 LLM 降到 1 次（约 1-1.5 秒） |
| AGENT_MAX_TOKENS | 当前 1536，复杂对比表容易被截断 | 调到 2048-4096 可解决截断导致的评测失败 |
| 29 元套餐测试用例 | 29 元套餐已下架，测试期望答案需更新 | 消除误报失败 |
| 流程型问答 | CRM 等流程型问题超出当前知识库覆盖范围 | 补充 CRM 流程文档到知识库 |
